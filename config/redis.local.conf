# Development cache only. Bind to this machine, never the local network.
bind 127.0.0.1
protected-mode yes
port 6379

# Cache contents are replaceable. Keep memory bounded and skip disk snapshots.
maxmemory 64mb
maxmemory-policy allkeys-lru
save ""
appendonly no
logfile ""
